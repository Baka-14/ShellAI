# Copy to ../.env or backend_example/.env
ELEVENLABS_API_KEY=
# Optional override (default is set in script)
# ELEVENLABS_AGENT_ID=
# Optional: auto-call end_session() after N seconds (e.g. ~90s advisor flow)
# CONVERSATION_MAX_SECONDS=95
# Optional: orchestrator cap (default 600 in script)
# ELEVENLABS_MAX_DURATION_SECONDS=600
